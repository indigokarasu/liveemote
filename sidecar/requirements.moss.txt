# MOSS-Transcribe-Diarize sidecar — install in a Python 3.12 venv.
#
#   uv venv --python 3.12 .venv && source .venv/bin/activate
#   cd <clone of https://github.com/OpenMOSS/MOSS-Transcribe-Diarize>
#   uv pip install -e ".[torch-runtime]" --torch-backend=auto
#   cd <repo root>
#   pip install -r sidecar/requirements.moss.txt
#
# Then run: python sidecar/moss_daemon.py --host 127.0.0.1 --port 8899
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
